<%= form_for(current_user.active_relationships.new, remote: true) do |form| %>
  <%= hidden_field_tag :followed_id, user.id %>
  <%= form.submit "Follow", class: "relationship-btn follow" %>
<% end %>